{
  "name": "ps1-company-explorer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.0",
    "multer": "^1.4.5-lts.1",
    "xlsx": "^0.18.5"
  }
}
